<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HealthyBackandNeck" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
        <item name ="android:statusBarColor">@color/login_color</item>
        <item name="drawerArrowStyle">@style/DrawerIcon</item>
        <item name="android:divider">@drawable/menu_item_padding</item>
        <item name="android:listDivider">@android:color/transparent</item>
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="Theme.HealthyBackandNeck" parent="Base.Theme.HealthyBackandNeck" />
    <style parent="Widget.Design.BottomSheet.Modal" name="BottomSheetStyle">
    <item name="android:background">@android:color/transparent</item>

</style>
    <style parent="Theme.Design.Light.BottomSheetDialog" name="BottomSheetDialogTheme">
    <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
</style>
    <style name="Apptheme.AppOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>
    <style name="Apptheme.popupOverlay" parent="ThemeOverlay.AppCompat.Light"/>
<style name="DrawerTextviewStyle" parent="android:Widget.TextView">
    <item name="android:fontFamily">serif</item>
    <item name="android:textSize">16sp</item>
    <item name="android:textColor">@color/white</item>
</style>
    <style name="DrawerIcon" parent="Widget.AppCompat.DrawerArrowToggle">
        <item name="color">@color/black</item>
    </style>
</resources>